/* pub fn delete_and_backspace(s: &mut String) {
    for v in s.chars().enumerate(){
        if v == '+' || v == '-'{
            v.replace(' ');
        }
    }
}

pub fn do_operations(v: &mut [String]) {
} */